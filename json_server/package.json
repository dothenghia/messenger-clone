{
  "name": "json_server",
  "version": "1.0.0",
  "description": "Fake rest API for my demo Messenger",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Do The Nghia",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.16.3"
  }
}
